rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // AskSAV Collection images
    // Each authenticated user can only access their own images.
    match /users/{userId}/asksavCollectionImages/{imageName} {
      allow read, write: if request.auth != null
                         && request.auth.uid == userId;
    }
  }
}